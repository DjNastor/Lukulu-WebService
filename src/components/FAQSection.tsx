
import React, { useState } from "react";
import { ChevronDownIcon } from "lucide-react";
const questions = [["How long will my website take?", "Most business websites take three to six weeks. Bigger custom tools can take longer."], ["Can I update my website?", "Yes. We show you how to update your website when the project is done."], ["Can you look after my website?", "Yes. We offer support for updates, backups, and small changes."]];
export function FAQSection() {const [openIndex, setOpenIndex] = useState<number | null>(0);return <section className="bg-white px-6 py-28 sm:py-32"><div className="mx-auto max-w-4xl"><div className="text-center"><p className="text-[11px] font-bold uppercase tracking-[0.17em] text-[#92400e]">Good to know</p><h2 className="mt-3 font-serif text-4xl tracking-[-0.04em] sm:text-5xl">Questions we hear often</h2></div><div className="mt-14 space-y-3">{questions.map(([question, answer], index) => {const open = openIndex === index;return <article className={`rounded-2xl border transition-colors ${open ? "border-[#c28d4b]" : "border-stone-200"}`} key={question}><h3><button type="button" aria-expanded={open} className="flex w-full items-center justify-between gap-6 px-6 py-5 text-left text-sm font-bold text-[#1a1817] sm:px-7" onClick={() => setOpenIndex(open ? null : index)}>{question}<ChevronDownIcon className={`shrink-0 text-[#b47732] transition-transform ${open ? "rotate-180" : ""}`} size={20} /></button></h3>{open && <p className="px-6 pb-6 text-sm leading-6 text-stone-500 sm:px-7">{answer}</p>}</article>;})}</div></div></section>;}